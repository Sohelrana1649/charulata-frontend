'use client';

import ProductDescriptionEditor, { ProductDescriptionEditorProps } from './ProductDescriptionEditor';

export default function RichTextEditor(props: ProductDescriptionEditorProps) {
  return <ProductDescriptionEditor {...props} />;
}
